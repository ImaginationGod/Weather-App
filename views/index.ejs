<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap');
        body{
            margin: 0;
            padding: 0;
            background-color: rgb(31, 31, 31);
        }
        
        /* Fonts */
        .poppins-regular {
            font-family: "Poppins", sans-serif;
            font-weight: 400;
            font-style: normal;
        }
        .poppins-medium {
            font-family: "Poppins", sans-serif;
            font-weight: 500;
            font-style: normal;
        }
        .poppins-semibold {
            font-family: "Poppins", sans-serif;
            font-weight: 600;
            font-style: normal;
        }
        .poppins-bold {
            font-family: "Poppins", sans-serif;
            font-weight: 700;
            font-style: normal;
        }

        /* Display */
        .container{
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }
        .inner{
            display: grid;
            align-items: center;
            justify-content: center;
            height: 90vh;
            width: 600px;
            background-color: rgb(15, 15, 15);
            border-radius: 15px;
            margin: auto;
            text-align: center;
            box-shadow: rgb(0 0 0 / 25%) 0 0 15px;
        }
        .info{
            color: hsl(0, 0%, 100%);
            height: 400px;
            width: 450px;
            border-radius: 15px;
            background-color: orange;
            display: flex;
            justify-content: space-between;
            line-height: 0.8;
            box-shadow: rgb(0 0 0 / 25%) 0 0 15px;
        }
        .one{
            display: inline-block;
            padding-top: 15px;
            padding-left: 15px;
            padding-right: 10px;
            text-align: left;
            border-radius: 15px;
            background-color: cornflowerblue;
            box-shadow: rgb(0 0 0 / 35%) 0 0 20px;
            line-height: 0.7;
            width: 50%;
        }
        .two{
            display: inline-block;
            padding-top: 20px;
            padding-left: 10px;
            padding-right: 15px;
            text-align: right;
            border-radius: 15px;
            width: 50%;
        }

        /* From Uiverse.io by barisdogansutcu */ 
        /* Search button */
        .search{
            padding: 10px 20px;
            margin-left: 5px;;
            border-radius: 50px;
            cursor: pointer;
            border: 0;
            background-color: white;
            box-shadow: rgb(0 0 0 / 5%) 0 0 8px;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            font-size: 15px;
            transition: all 0.5s ease;
        }
        .search:hover{
            letter-spacing: 3px;
            background-color: hsl(261deg 80% 48%);
            color: hsl(0, 0%, 100%);
            box-shadow: rgb(93 24 220) 0px 7px 29px 0px;
        }
        .search:active{
            letter-spacing: 3px;
            background-color: hsl(261deg 80% 48%);
            color: hsl(0, 0%, 100%);
            box-shadow: rgb(93 24 220) 0px 0px 0px 0px;
            transform: translateY(10px);
            transition: 100ms;
        }

        /* Mode button */
        .mode{
            padding: 10px 20px;
            margin-left: 5px;
            border-radius: 50px;
            cursor: pointer;
            border: 0;
            background-color: white;
            box-shadow: rgb(0 0 0 / 5%) 0 0 8px;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            font-size: 10px;
            transition: all 0.5s ease;
        }
        .mode:hover{
            letter-spacing: 3px;
            background-color: hsl(345, 80%, 48%);
            color: hsl(0, 0%, 100%);
            box-shadow: rgb(220, 24, 73) 0px 7px 29px 0px;
        }
        .mode:active{
            letter-spacing: 3px;
            background-color: hsl(345, 80%, 48%);
            color: hsl(0, 0%, 100%);
            box-shadow: rgb(220, 24, 73) 0px 7px 29px 0px;
            transform: translateY(10px);
            transition: 100ms;
        }

        /* Input */
        .input{
            color: hsl(0, 0%, 100%);
            border: 3px solid #8707ff;
            border-radius: 25px;
            padding: 10px 25px;
            max-width: 190px;
            background: transparent;
        }
        .input:hover{
            border-color: hsl(261deg 80% 48%);
            color: hsl(0, 0%, 100%);
            box-shadow: rgb(93 24 220) 0px 7px 29px 0px;
        }
        .input:active{
            box-shadow: rgb(93 24 220) 0px 7px 29px 0px;
        }
        .input:focus{
            outline: none;
            box-shadow: rgb(93 24 220) 0px 7px 29px 0px;
        }

        /* Light Mode */
        .light-mode-outer{
            color: rgb(17, 17, 17);
            background-color: white;
        }
        .light-mode-inner{
            color: rgb(17, 17, 17);
            background-color: #F2F0EF;
        }
        .trans{
            transition: all 0.3s ease;
        }
        .light-mode-btn{
            color: white;
            background-color: rgb(17, 17, 17);
        }
        .light-mode-input{
            color: rgb(17, 17, 17);
        }
        .light-mode-input:hover{
            color: rgb(17, 17, 17);
        }
        .light-mode-loader{
            background-color: #F2F0EF !important;
        }
        .icon_parent{
            display: flex;
            align-items: center;
        }

        /*loading screen*/
        .loader{
            height: 100vh;
            width: 100vw;
            display: none;
            justify-content: center;
            align-items: center;
            position: absolute;
            background-color: rgb(17, 17, 17);
            z-index: 5;
        }
        /* From Uiverse.io by G4b413l */ 
        .three-body {
            --uib-size: 35px;
            --uib-speed: 0.8s;
            --uib-color: #5D3FD3;
            position: relative;
            display: inline-block;
            height: var(--uib-size);
            width: var(--uib-size);
            animation: spin78236 calc(var(--uib-speed) * 2.5) infinite linear;
        }
        .three-body__dot {
            position: absolute;
            height: 100%;
            width: 30%;
        }
        .three-body__dot:after {
            content: '';
            position: absolute;
            height: 0%;
            width: 100%;
            padding-bottom: 100%;
            background-color: var(--uib-color);
            border-radius: 50%;
        }
        .three-body__dot:nth-child(1) {
            bottom: 5%;
            left: 0;
            transform: rotate(60deg);
            transform-origin: 50% 85%;
        }
        .three-body__dot:nth-child(1)::after {
            bottom: 0;
            left: 0;
            animation: wobble1 var(--uib-speed) infinite ease-in-out;
            animation-delay: calc(var(--uib-speed) * -0.3);
        }
        .three-body__dot:nth-child(2) {
            bottom: 5%;
            right: 0;
            transform: rotate(-60deg);
            transform-origin: 50% 85%;
        }
        .three-body__dot:nth-child(2)::after {
            bottom: 0;
            left: 0;
            animation: wobble1 var(--uib-speed) infinite
                calc(var(--uib-speed) * -0.15) ease-in-out;
        }
        .three-body__dot:nth-child(3) {
            bottom: -5%;
            left: 0;
            transform: translateX(116.666%);
        }
        .three-body__dot:nth-child(3)::after {
            top: 0;
            left: 0;
            animation: wobble2 var(--uib-speed) infinite ease-in-out;
        }
        @keyframes spin78236 {
            0% {
            transform: rotate(0deg);
            }
            100% {
            transform: rotate(360deg);
            }
        }
        @keyframes wobble1 {
            0%,
            100% {
            transform: translateY(0%) scale(1);
            opacity: 1;
            }
            50% {
            transform: translateY(-66%) scale(0.65);
            opacity: 0.8;
            }
        }
        @keyframes wobble2 {
            0%,
            100% {
            transform: translateY(0%) scale(1);
            opacity: 1;
            }
            50% {
            transform: translateY(66%) scale(0.65);
            opacity: 0.8;
            }
        }
    </style>
</head>
<body>
    <div id="loader" class="loader"><%- include('loader'); %></div>
    <div class="container">
        <div id="inn" class="inner trans">
            <form action="/weather" method="post">
                <input id="inp" class="input poppins-medium trans" name="search_city" type="text" required="text" placeholder="Enter city name" autocomplete="off">
                <button class="poppins-medium search" type="submit">Search</button>
                <%- include('mode'); %>
            </form>
            <div class="info">
                <%- include('card', {city: city, ctemp: ctemp, maxtemp: maxtemp, mintemp: mintemp, ftemp: ftemp, hum: hum, winds: winds,  wtype: wtype, wicon: wicon, wtype_des: wtype_des}); %>
            </div>
        </div>
    </div>
    <script>
        document.body.className = 'trans';
        let btn = document.getElementById('lm');
        let loader = document.getElementById('loader');

        let check = false;
        if(sessionStorage.getItem('mode') == 'light'){ //When being re-directed back to this page
            document.body.classList.toggle('light-mode-outer'); //For background
            document.getElementById('inn').classList.toggle('light-mode-inner'); //For inner background
            document.getElementById('inp').classList.toggle('light-mode-input'); //For input field
            btn.classList.toggle('light-mode-btn');
            btn.innerText = 'Dark';
            check = true;
            loader.classList.toggle('light-mode-loader');
        }
        let mode = ()=>{
            document.body.classList.toggle('light-mode-outer'); //For background
            document.getElementById('inn').classList.toggle('light-mode-inner'); //For inner background
            document.getElementById('inp').classList.toggle('light-mode-input'); //For input field
            btn.classList.toggle('light-mode-btn');
            loader.classList.toggle('light-mode-loader');
            if(check==false){ //Light mode
                btn.innerText = 'Dark';
                check = true;
                sessionStorage.setItem("mode","light");
            }
            else{ //Dark mode
                btn.innerText = 'Light';
                check = false;
                sessionStorage.setItem("mode","dark");
            }
        }
        let load = ()=>{
            loader.style.display = 'flex';
            let b = setTimeout(()=>{loader.style.display = 'none';}, 600);
        }
        // load(); //-> For asthetics only
        document.onreadystatechange = function () {
            if (document.readyState !== "complete") {
                loader.style.display = 'flex';
            } else {
                loader.style.display = 'none';
            }
        };
    </script>
</body>
</html>